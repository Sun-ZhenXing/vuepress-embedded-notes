import{_ as p,M as t,p as l,q as d,R as a,N as e,V as o,t as s,a1 as i}from"./framework-cdc56dbd.js";const r={},u=a("h1",{id:"emmc-系统",tabindex:"-1"},[a("a",{class:"header-anchor",href:"#emmc-系统","aria-hidden":"true"},"#"),s(" eMMC 系统")],-1),h={class:"table-of-contents"},k=a("h2",{id:"_1-烧录系统",tabindex:"-1"},[a("a",{class:"header-anchor",href:"#_1-烧录系统","aria-hidden":"true"},"#"),s(" 1. 烧录系统")],-1),b={href:"https://www.waveshare.net/wiki/JETSON-NANO-DEV-KIT",target:"_blank",rel:"noopener noreferrer"},g=i(`<h2 id="_2-复制系统到-u-盘" tabindex="-1"><a class="header-anchor" href="#_2-复制系统到-u-盘" aria-hidden="true">#</a> 2. 复制系统到 U 盘</h2><p>查看 U 盘的名称：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /dev/sd*
</code></pre></div><p>这里可以看到设备名称为 <code>sda</code>，设置其为 <code>usb_disk</code>：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">export</span> <span class="token assign-left variable">usb_name</span><span class="token operator">=</span>sda
<span class="token builtin class-name">export</span> <span class="token assign-left variable">usb_disk</span><span class="token operator">=</span>/dev/<span class="token variable">\${usb_name}</span>
</code></pre></div><p>查看其是否被挂载：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">df</span> <span class="token parameter variable">-h</span> <span class="token operator">|</span> <span class="token function">grep</span> <span class="token variable">\${usb_disk}</span>
</code></pre></div><p>如果被挂载，可以先卸载：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">umount</span> /media/<span class="token variable">\${mount_dir}</span>
</code></pre></div><p>现在格式化此 U 盘：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> mkfs.ext4 <span class="token variable">\${usb_disk}</span>
</code></pre></div><p>如果提示是否格式化，按 <code>y</code> 回车，耐心等待。</p><p>下面将 <code>/boot/extlinux/extlinux.conf</code> 文件的下面一行修改为从 <code>sda</code> 引导：</p><div class="language-diff" data-ext="diff"><pre class="language-diff"><code><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">       APPEND \${cbootargs} quiet root=/dev/mmcblk0p1 rw rootwait ...
</span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">       APPEND \${cbootargs} quiet root=/dev/sda rw rootwait ...
</span></span></code></pre></div><p>可以通过 <code>sed</code> 直接修改：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">sed</span> <span class="token parameter variable">-i</span> s/mmcblk0p1/<span class="token variable">\${usb_name}</span>/ /boot/extlinux/extlinux.conf
</code></pre></div><p>将 U 盘挂载到 <code>/mnt/</code>：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">mount</span> <span class="token variable">\${usb_disk}</span> /mnt
</code></pre></div><p>将系统全部文件复制到 U 盘：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">cp</span> <span class="token parameter variable">-ax</span> / /mnt
</code></pre></div><p>此过程较长，请耐心等待。</p><p>下面卸载 U 盘：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">umount</span> /mnt/
</code></pre></div><p>重启系统：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">reboot</span>
</code></pre></div>`,25);function f(_,v){const n=t("router-link"),c=t("ExternalLinkIcon");return l(),d("div",null,[u,a("nav",h,[a("ul",null,[a("li",null,[e(n,{to:"#_1-烧录系统"},{default:o(()=>[s("1. 烧录系统")]),_:1})]),a("li",null,[e(n,{to:"#_2-复制系统到-u-盘"},{default:o(()=>[s("2. 复制系统到 U 盘")]),_:1})])])]),k,a("p",null,[s("将系统烧录到 eMMC，可参考 "),a("a",b,[s("SDK Manager 的使用"),e(c)]),s("。")]),g])}const x=p(r,[["render",f],["__file","emmc.html.vue"]]);export{x as default};
